
/**
 * Generated by Sideko (sideko.dev)
 **/


package com.yourorg.brazejava;

import com.yourorg.brazejava.core.ApiException;
import com.yourorg.brazejava.resources.email.unsubscribes.params.ListRequest;
import org.junit.jupiter.api.Test;


public final class EmailUnsubscribesTestClient {
    @Test
    void testList200SuccessAllParams() {
        Client client = Client
                        .builder()
                        .environment(Environment.MOCK_SERVER)
                        .withBearerAuth("API_TOKEN")
                        .build();

        try {
            client.email().unsubscribes().list(ListRequest
                                               .builder()
                                               .email("example@braze.com")
                                               .endDate("2020-02-01")
                                               .limit(1)
                                               .offset(1)
                                               .sortDirection("desc")
                                               .startDate("2020-01-01")
                                               .build());
        } catch (ApiException e) {
            System.err.println("Error msg: " + e.toString());
            throw e;
        }
    }
    @Test
    void testList200SuccessRequiredOnly() {
        Client client = Client
                        .builder()
                        .environment(Environment.MOCK_SERVER)
                        .withBearerAuth("API_TOKEN")
                        .build();

        try {
            client.email().unsubscribes().list(ListRequest
                                               .builder()
                                               .email("example@braze.com")
                                               .endDate("2020-02-01")
                                               .limit(1)
                                               .offset(1)
                                               .sortDirection("desc")
                                               .startDate("2020-01-01")
                                               .build());
        } catch (ApiException e) {
            System.err.println("Error msg: " + e.toString());
            throw e;
        }
    }
}
