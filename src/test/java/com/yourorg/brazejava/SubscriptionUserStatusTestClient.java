
/**
 * Generated by Sideko (sideko.dev)
 **/


package com.yourorg.brazejava;

import com.yourorg.brazejava.core.ApiException;
import com.yourorg.brazejava.resources.subscription.user.status.params.ListRequest;
import org.junit.jupiter.api.Test;


public final class SubscriptionUserStatusTestClient {
    @Test
    void testList200SuccessAllParams() {
        Client client = Client
                        .builder()
                        .environment(Environment.MOCK_SERVER)
                        .withBearerAuth("API_TOKEN")
                        .build();

        try {
            client.subscription().user().status().list(ListRequest
                    .builder()
                    .externalId("{{external_id}}")
                    .limit(100)
                    .offset(1)
                    .phone("+11112223333")
                    .build());
        } catch (ApiException e) {
            System.err.println("Error msg: " + e.toString());
            throw e;
        }
    }
    @Test
    void testList200SuccessRequiredOnly() {
        Client client = Client
                        .builder()
                        .environment(Environment.MOCK_SERVER)
                        .withBearerAuth("API_TOKEN")
                        .build();

        try {
            client.subscription().user().status().list(ListRequest
                    .builder()
                    .externalId("{{external_id}}")
                    .limit(100)
                    .offset(1)
                    .phone("+11112223333")
                    .build());
        } catch (ApiException e) {
            System.err.println("Error msg: " + e.toString());
            throw e;
        }
    }
}
