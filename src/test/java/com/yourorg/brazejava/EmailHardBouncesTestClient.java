
/**
 * Generated by Sideko (sideko.dev)
 **/


package com.yourorg.brazejava;

import com.yourorg.brazejava.core.ApiException;
import com.yourorg.brazejava.resources.email.hardbounces.params.ListRequest;
import org.junit.jupiter.api.Test;


public final class EmailHardBouncesTestClient {
    @Test
    void testList200SuccessAllParams() {
        Client client = Client
                        .builder()
                        .environment(Environment.MOCK_SERVER)
                        .withBearerAuth("API_TOKEN")
                        .build();

        try {
            client.email().hardBounces().list(ListRequest
                                              .builder()
                                              .email("example@braze.com")
                                              .endDate("2019-02-01")
                                              .limit(100)
                                              .offset(1)
                                              .startDate("2019-01-01")
                                              .build());
        } catch (ApiException e) {
            System.err.println("Error msg: " + e.toString());
            throw e;
        }
    }
    @Test
    void testList200SuccessRequiredOnly() {
        Client client = Client
                        .builder()
                        .environment(Environment.MOCK_SERVER)
                        .withBearerAuth("API_TOKEN")
                        .build();

        try {
            client.email().hardBounces().list(ListRequest
                                              .builder()
                                              .email("example@braze.com")
                                              .endDate("2019-02-01")
                                              .limit(100)
                                              .offset(1)
                                              .startDate("2019-01-01")
                                              .build());
        } catch (ApiException e) {
            System.err.println("Error msg: " + e.toString());
            throw e;
        }
    }
}
